# Suggested Commands

## Development
```bash
# Start development server
pnpm dev

# Build for production
pnpm build

# Start production server
pnpm start

# Lint code
pnpm lint
```

## Database (Supabase)
```bash
# Run migrations manually (copy SQL to Supabase dashboard)
# Migrations are in supabase/migrations/

# Seed data
# Use supabase/seed.sql
```

## Darwin System Commands
```bash
# Standard unix commands work
ls, cd, grep, find, cat, etc.
```
